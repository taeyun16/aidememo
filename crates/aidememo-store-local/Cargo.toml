[package]
name = "aidememo-store-local"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
description = "Single-node SQLite command ledger for AideMemo server and SSOT mode"
license.workspace = true
repository.workspace = true
version.workspace = true
publish = false

[dependencies]
aidememo-domain = { version = "0.1.0", path = "../aidememo-domain" }
rusqlite = { version = "0.40.1", default-features = false, features = ["bundled"] }
serde_json = "1.0"

[dev-dependencies]
aidememo-service = { version = "0.1.0", path = "../aidememo-service" }
tempfile = "3.10"

[lints]
workspace = true
